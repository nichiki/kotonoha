[model]
name = "kotoba-whisper-v2.0-ggml"  # デフォルトはGGML版
auto_download = true

[general]
output_format = "srt"

[vad]
threshold = 0.30
min_speech_duration_ms = 100
min_silence_duration_ms = 300
speech_pad_ms = 50